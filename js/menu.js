// Menu interactions: navegación desde el menú principal
document.addEventListener('DOMContentLoaded', function () {
	console.log('🧭 menu.js loaded');

	// Nota: no manejamos el botón de `levels` aquí para evitar listeners duplicados.
	// `main.js` ahora es el responsable de redirigir a la pantalla de niveles.

	// Nota: no añadimos comportamiento de inicio aquí — `main.js` gestiona el botón "Iniciar juego".
});
